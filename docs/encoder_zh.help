该工具用于对地理空间影像进行预处理，并使用 SAM 图像编码器生成影像特征

生成的影像特征可在 Geo-SAM 工具中配合点提示和框选提示使用，以标注地貌目标

选择要处理的栅格图层或影像文件后，还需要选择合适的波段
SAM 原生仅支持三波段 RGB 影像，但本工具也支持单波段或双波段影像，因此可以尝试灰度影像或 NDVI 等光谱指数影像

由于 SAM 仅支持固定尺寸的输入影像，小影像会被缩放到模型输入尺寸，大影像则会按网格方式切分为带重叠的分块
`stride` 参数用于控制重叠方式，`overlap = patch_size - stride`

输入给 SAM 的影像数值范围应为 0 到 255
如果原始影像不在该范围内，可以指定一个数值范围并重缩放到 [0, 255]
默认情况下，工具会自动估计影像数据的最小值和最大值，并将 [min, max] 重缩放到 [0, 255]

GeoSAM 模型检查点需要提前在 Geo-SAM 设置中下载
在编码器中选择 GeoSAM 模型后，工具会从已配置的模型目录中解析对应的检查点
如果所选模型尚未下载，请改选已下载模型，或先在设置中下载后再重试

加快编码速度的建议

1. 选择更小的处理范围
2. 降低目标分辨率
3. 增大 stride 以减少重叠区域
4. 选择更小的 GeoSAM 模型
5. 在显存充足时使用 GPU
6. 如果更关注峰值内存而不是速度，可选择 低内存模式
